syntax = "proto3";

import 'brewblox.proto';
import "nanopb.proto";

package blox;

message SetPointSimple {
    option (nanopb_msgopt).msgid = 300;
    sint32 setting = 1 [(brewblox).unit = "degC", (brewblox).scale = 256, (nanopb).int_size = IS_16];
}
