syntax = "proto3";

import 'brewblox.proto';

package blox;

message OneWireTempSensor {
  sint32 value = 1 [(brewblox).unit = "degC", (brewblox).fraction_bits = 8];;
  bool connected = 2;
  sint32 offset = 3 [(brewblox).unit = "degC", (brewblox).fraction_bits = 8];
  fixed64 address = 4;
}
